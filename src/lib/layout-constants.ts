/**
 * Shared marketing layout tokens (about-us audit: promote FIXED_WIDTH for reuse).
 */
export const MARKETING_SECTION_WIDTH =
  "mx-auto max-w-8xl px-8 sm:px-10 md:px-14 lg:px-20";

/** Approximate offset below fixed pill navigation — tune with CSS var when nav height is tokenized */
export const MARKETING_MAIN_TOP_PADDING = "pt-28 md:pt-24";
